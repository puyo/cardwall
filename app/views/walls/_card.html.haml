= div_for(card, style: card_style(card)) do
  .content{style: "background-color: #{card.bg_color}; width: #{card.w}px; min-height: #{card.h}px", class: card_classes(card)}
    .text<
      :preserve
        #{h(card.text)}
  .edit_card_form{id: dom_id(card, 'edit')}
    = form.fields_for :cards, card do |f|
      .field.x
        = f.label :x
        = f.text_field :x, class: 'x_input'
      .field.y
        = f.label :y
        = f.text_field :y, class: 'y_input'
      .field.z
        = f.label :z
        = f.text_field :z, class: 'z_input'
      .field.w
        = f.label :w
        = f.text_field :w, class: 'w_input'
      .field.h
        = f.label :h
        = f.text_field :h, class: 'h_input'
      .field.text
        = f.label :text
        = f.text_area :text, rows: 5, class: 'text_input'
      .field.author
        = f.label :author
        = f.text_field :author
      .field.bg_color
        = f.label :bg_color, 'Background'
        = f.text_field :bg_color, class: 'bg_color_input'
      .field.shadow
        = f.label :shadow
        = f.check_box :shadow, class: 'shadow_input'
    .instructions
      %p esc = close
